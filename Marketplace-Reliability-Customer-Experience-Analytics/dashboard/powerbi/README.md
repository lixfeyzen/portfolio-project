# Power BI File

Place the final Power BI report file in this folder after building the dashboard manually.

Suggested file name:

```text
Marketplace_Reliability_Customer_Experience.pbix
```

Do not create or commit a placeholder `.pbix` file. Only add the real Power BI file after it has been built and checked.

Before building the report, run the SQL workflow and validate totals against `documentation/sql_execution_outputs/` if SQL Server is reachable.

Recommended supporting docs:

- `documentation/powerbi_build_guide.md`
- `documentation/powerbi_measures.md`
- `sql/06_powerbi_query_reference.sql`
